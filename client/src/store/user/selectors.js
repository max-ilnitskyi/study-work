// import { createSelector } from 'reselect';

// Get user data object
export const user = state => state.user.user;

// Get user fetch state
export const userFetchState = state => state.user.userFetchState;
